import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: { flex: 1 },
  camera: { flex: 1 },
  text: {
    position: 'absolute',
    bottom: 30,
    alignSelf: 'center',
    color: '#fff',
    fontSize: 16,
  },
});

export default styles